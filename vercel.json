{
  "framework": "nextjs",
  "installCommand": "npm install && cd core && npm install",
  "buildCommand": "cd core && npx prisma generate && next build --no-turbopack",
  "outputDirectory": "core/.next"
}
