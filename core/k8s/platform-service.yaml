apiVersion: v1
kind: Service
metadata:
  name: resilience-platform-svc
  namespace: default
  labels:
    app: resilience-platform
spec:
  type: ClusterIP
  selector:
    app: resilience-platform
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
