{
  "name": "resilience-platform",
  "private": true,
  "scripts": {
    "build": "cd core && npm install && npx prisma generate && npx next build"
  },
  "dependencies": {
    "next": "16.2.10"
  }
}
